<% if user_signed_in? %>
  <h1>Your Items</h1>
  <% @items.each do |item| %>
    <h3><%= link_to item.title, item %> </h3>
    <p><%= item.description %></p>
  <% end %>
<% else %>
  <%= render 'welcome_page' %>  
<% end %>
